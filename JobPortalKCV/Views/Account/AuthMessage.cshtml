@model string

@{
    ViewBag.Title = "Account";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container pt-120 pb-120">
    <div class="alert alert-info">@Model</div>
    @Html.ActionLink("Go to login", "Login", null, new { @class = "btn" })
</div>
